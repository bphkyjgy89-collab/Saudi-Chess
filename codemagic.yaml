workflows:
  saudi-chess-ios:
    name: Saudi Chess - iOS Build and Publish
    max_build_duration: 60
    instance_type: mac_mini_m2
    integrations:
      app_store_connect: SaudiChess_ASC_Key   # غيّرها لاسم مفتاح ASC عندك بـ Codemagic
    environment:
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.saudichess.app
      vars:
        APP_STORE_APPLE_ID: 0000000000        # حط Apple ID الرقمي بعد إنشاء التطبيق بـ App Store Connect
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: Set up code signing settings on Xcode project
        script: |
          xcode-project use-profiles
      - name: Get Flutter packages
        script: |
          flutter packages pub get
      - name: Install pods
        script: |
          find . -name "Podfile" -execdir pod install \;
      - name: Flutter build ipa
        script: |
          flutter build ipa --release \
            --export-options-plist=/Users/builder/export_options.plist
    artifacts:
      - build/ios/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - flutter_drive.log
    publishing:
      app_store_connect:
        auth: integration
        submit_to_testflight: true
        submit_to_app_store: false
